---

---

<section
  id="contact-form"
  class="py-12 sm:py-16 bg-white"
  aria-labelledby="contact-heading"
>
  <div class="max-w-2xl mx-auto px-4 sm:px-6">
    <div class="text-center">
      <h2
        id="contact-heading"
        class="text-3xl font-extrabold text-gray-900 sm:text-4xl uppercase"
      >
        Contacta con Nosotros
      </h2>
      <p class="mt-4 text-lg leading-6 text-gray-500">
        ¿Tienes alguna pregunta? Rellena el formulario y nos pondremos en
        contacto contigo lo antes posible.
      </p>
    </div>

    <form
      action="#"
      method="POST"
      class="mt-12 grid grid-cols-1 gap-y-6 sm:grid-cols-2 sm:gap-x-8"
    >
      <div class="sm:col-span-1">
        <label
          for="name"
          class="block text-sm font-bold text-gray-700 uppercase">Nombre</label
        >
        <div class="mt-1">
          <input
            type="text"
            name="name"
            id="name"
            autocomplete="name"
            required
            placeholder="Tu nombre completo"
            class="text-gray-900 py-3 px-4 block w-full shadow-sm focus:ring-(--brand-green) focus:border-(--brand-green) border-gray-300 rounded-md transition-all"
          />
        </div>
      </div>

      <div class="sm:col-span-1">
        <label
          for="email"
          class="block text-sm font-bold text-gray-700 uppercase">Email</label
        >
        <div class="mt-1">
          <input
            id="email"
            name="email"
            type="email"
            autocomplete="email"
            required
            placeholder="tu.email@ejemplo.com"
            class="text-gray-900 py-3 px-4 block w-full shadow-sm focus:ring-(--brand-green) focus:border-(--brand-green) border-gray-300 rounded-md transition-all"
          />
        </div>
      </div>

      <div class="sm:col-span-1">
        <label
          for="phone"
          class="block text-sm font-bold text-gray-700 uppercase"
          >Teléfono <span class="text-gray-400 font-normal lowercase"
            >(Opcional)</span
          ></label
        >
        <div class="mt-1">
          <input
            type="text"
            name="phone"
            id="phone"
            autocomplete="tel"
            placeholder="600 000 000"
            class="text-gray-900 py-3 px-4 block w-full shadow-sm focus:ring-(--brand-green) focus:border-(--brand-green) border-gray-300 rounded-md transition-all"
          />
        </div>
      </div>

      <div class="sm:col-span-1">
        <label for="age" class="block text-sm font-bold text-gray-700 uppercase"
          >Edad</label
        >
        <div class="mt-1">
          <input
            type="number"
            name="age"
            id="age"
            min="14"
            max="99"
            required
            placeholder="Ej: 25"
            class="text-gray-900 py-3 px-4 block w-full shadow-sm focus:ring-(--brand-green) focus:border-(--brand-green) border-gray-300 rounded-md transition-all"
          />
        </div>
      </div>

      <div class="sm:col-span-2">
        <fieldset>
          <legend class="block text-sm font-bold text-gray-700 uppercase"
            >Género</legend
          >
          <div class="mt-2 grid grid-cols-1 sm:grid-cols-3 gap-4">
            <div class="relative">
              <input
                id="gender-male"
                name="gender"
                type="radio"
                value="male"
                class="peer sr-only"
              />
              <label
                for="gender-male"
                class="flex items-center justify-center text-sm font-bold py-3 px-3 rounded-md cursor-pointer transition-all border border-gray-300 bg-white text-gray-700 peer-checked:bg-(--brand-green) peer-checked:text-zinc-100 peer-checked:border-(--brand-green) shadow-sm"
              >
                Masculino
              </label>
            </div>

            <div class="relative">
              <input
                id="gender-female"
                name="gender"
                type="radio"
                value="female"
                class="peer sr-only"
              />
              <label
                for="gender-female"
                class="flex items-center justify-center text-sm font-bold py-3 px-3 rounded-md cursor-pointer transition-all border border-gray-300 bg-white text-gray-700 peer-checked:bg-(--brand-green) peer-checked:text-zinc-100 peer-checked:border-(--brand-green) shadow-sm"
              >
                Femenino
              </label>
            </div>

            <div class="relative">
              <input
                id="gender-none"
                name="gender"
                type="radio"
                value="none"
                checked
                class="peer sr-only"
              />
              <label
                for="gender-none"
                class="flex items-center justify-center text-sm font-bold py-3 px-3 rounded-md cursor-pointer transition-all border border-gray-300 bg-white text-gray-700 peer-checked:bg-(--brand-green) peer-checked:text-zinc-100 peer-checked:border-(--brand-green) shadow-sm"
              >
                No contestar
              </label>
            </div>
          </div>
        </fieldset>
      </div>

      <div class="sm:col-span-2">
        <label
          for="message"
          class="block text-sm font-bold text-gray-700 uppercase">Mensaje</label
        >
        <div class="mt-1">
          <textarea
            id="message"
            name="message"
            rows="4"
            required
            placeholder="Cuéntanos tus objetivos..."
            class="text-gray-900 py-3 px-4 block w-full shadow-sm focus:ring-(--brand-green) focus:border-(--brand-green) border-gray-300 rounded-md transition-all"
          ></textarea>
        </div>
      </div>

      <div class="sm:col-span-2 flex justify-center">
        <button
          type="submit"
          class="w-50 inline-flex items-center justify-center px-4 py-2 border border-transparent rounded-md shadow-xl text-base font-black text-zinc-150 bg-(--brand-green) hover:bg-(--brand-green-dark) hover:scale-[1.01] active:scale-[0.98] transition-all focus:outline-none uppercase"
        >
          Enviar
        </button>
      </div>
    </form>
  </div>
</section>
